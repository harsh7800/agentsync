{
  "name": "@agent-sync/e2e",
  "version": "1.2.2",
  "description": "End-to-end tests for AgentSync CLI",
  "private": true,
  "scripts": {
    "test": "vitest --testPathPattern=e2e"
  },
  "dependencies": {
    "@agent-sync/cli": "workspace:*",
    "@agent-sync/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "vitest": "^1.1.0"
  }
}
